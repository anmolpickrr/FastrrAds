# Static creative uploads

Drop new static creative images here (via GitHub's **Add file → Upload files**,
not "Create new file" — that only creates empty text files, not real images).

A routine checks this folder periodically and automatically wires any new
images into the live Creative Showcase, then removes them from this folder
once they're integrated into `assets/creative-data.js`.
